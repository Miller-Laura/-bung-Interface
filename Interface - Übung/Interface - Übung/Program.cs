namespace Interface___Übung;


//Interface
interface IZahlbar
{
    decimal BerechnePreis();
}

/*Erstelle ein Interface IZahlbar, das:
eine Methode decimal BerechnePreis() enthält.
    Diese Methode gibt den Preis des jeweiligen Objekts zurück.

    🧾 Teil 3 – Hauptprogramm
    Erstelle im Hauptprogramm eine Liste von IZahlbar-Objekten, z. B.:

Füge einige Produkt- und Dienstleistung-Objekte hinzu und gib für jedes Objekt den Preis aus.
    Berechne außerdem die Gesamtsumme aller Objekte in der Liste.
    List<IZahlbar> einkauf = new List<IZahlbar>();*/